# 規則

所有規則必須同時滿足。任何一條不滿足，就是實作的問題，不是規則或實機的問題。

## 一、實機規則

1. 實機永遠走得過去。程式算出「走不到」或「繞不過」，一定是實作理解錯了。
2. 禁止穿牆。
3. TKG 算出來的座標絕對正確。所有物件都放在 TKG 座標上，不得挪動。
4. 所有物件出現在通道裡時，必定在通道中央。
5. 一層樓只有一個上樓梯、一個下樓梯。
6. 尺寸（以遺跡地形為準）：
   - 通道寬度 8px。
   - 下樓梯佔通道寬度 1/3（8/3px）。
   - 人佔通道寬度 1/4（2px），身體是 2px × 2px。
   - 寶箱跟人一樣大，2px × 2px。
7. 下樓梯是正方形，沒有方向。TKG 給下樓梯標方向是它唯一的失誤。
8. 上樓梯是長方形，有方向（TKG 已正確標示）：
   - 朝向那邊 2px 寬，深 4px。
   - 起點在它朝向的正前方。起點也要考慮角色身體的大小。
   - 反方向走時，不准從它屁股開始走。必須從起點出發，先往左或往右，再繼續原來的路線。
9. 寶箱：
   - 角色剛下樓那一刻，可以跟寶箱重疊。
   - 除此之外任何情況都不准重疊；一經走動，寶箱就是障礙物。
   - 繞過寶箱時禁止切斜角。
   - 打開時一定要貼著寶箱的一條邊，不能斜著開。
   - 開寶箱也要看實際行走的路線會不會被擋。
10. 路線不准踩上樓梯（終點除外）。

## 二、單位

1. px 指 TKG 畫布的 px，16px = 1 tile。
2. 16px 對齊 A* 的一步（1 tile 邊長）。
3. 上樓梯座標要用程式碼算。

## 三、程式碼與版權

1. TKG 的 JavaScript 程式碼一行也不准動。已經做好的顯示層（`scaleRenderedOutput` 的顯示接口、`<script>` 標籤）可以保留。
2. 能從 TKG 程式碼直接讀到的東西，一律從 TKG 讀，自己的一個不留。
3. 不准使用 DQ9AT 的地圖生成器（有版權問題）。
   - 能被 TKG 取代的部分，必須完全由 TKG 取代。
   - 不能取代的部分必須改寫（有引用條件）。
   - 表格一律改名為 `D_*`（例如 TableB 改成 D_B）。
4. TKG 程式碼以外的所有註解都去掉。
5. 畫面上禁止出現「DQ9AT」。
6. 去掉死碼、死參數，以及經驗證能無損去除的重複邏輯。
7. 叫你拿掉甚麼，就只拿掉那個，別的不要動。

## 四、介面

1. 介面文字用英文，用正確的術語。
2. 字號不得偏離 TKG 原版。
3. 說明不准佔版面，一律用 ⓘ 提示呈現。不是刪掉。
4. 選項框手機一行 3 個；PC 用同樣的尺寸，一行可以放好幾個。
5. 路線盡量走直線和 45°，少貼牆走。

## 五、做事方式

1. 禁止靠讀圖盲猜。一切從程式碼算。
2. 可以開啟驗證：用程式對規則做檢查（包括批量），不滿足的就當成實作的問題修掉。
3. TKG 程式碼沒寫的資料，直接說沒寫。
4. 不准問顯而易見的問題。
5. 用繁體中文回覆。
